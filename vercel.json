{
    "version": 2,
    "builds": [
      {
        "src": "app.py",
        "use": "@vercel/python",
      "config": {
        "installCommand": "apt-get update && apt-get install -y build-essential && pip install -r requirements.txt"
      }
    }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "app.py"
      }
    ]
  }